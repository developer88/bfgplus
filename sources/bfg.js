// Generated by CoffeeScript 1.4.0
(function() {
  var Bfg, GooglePlus;

  GooglePlus = (function() {

    function GooglePlus(options) {
      this.options = options;
    }

    GooglePlus.prototype.posts = function() {};

    GooglePlus.prototype.post = function(id) {};

    return GooglePlus;

  })();

  Bfg = (function() {

    function Bfg(options) {
      var option;
      this.options = options;
      option = {
        resGetPath: 'js/locales/__lng__.json',
        lng: 'en-US'
      };
      $.i18n.init(option, function(t) {
        var appName;
        return appName = t("key");
      });
      this.gplus = new GooglePlus;
    }

    Bfg.prototype.plus = function() {
      return this.gplus;
    };

    Bfg.prototype.load_blog = function(dom_obj) {
      var post, posts;
      posts = plus.posts;
      if (posts.size > 0) {
        return render_posts(dom_obj, (function() {
          var _i, _len, _results;
          _results = [];
          for (_i = 0, _len = posts.length; _i < _len; _i++) {
            post = posts[_i];
            _results.push(pack_post(post));
          }
          return _results;
        })());
      } else {
        return message($.t('app.messages.posts.empty'));
      }
    };

    Bfg.prototype.load_post = function(id) {
      var post, post_html;
      if (parseInt(id) === 0) {
        return message($.t('app.messages.posts.not_found'));
      }
      post = plus.post(id);
      post_html = unpack_post(post);
      return render_post(post_html);
    };

    Bfg.prototype.message = function(text) {};

    Bfg.prototype.pack_post = function(post) {};

    Bfg.prototype.unpack_post = function(post) {};

    Bfg.prototype.render_posts = function(dom_obj, html) {};

    Bfg.prototype.render_post = function(html) {};

    return Bfg;

  })();

}).call(this);
